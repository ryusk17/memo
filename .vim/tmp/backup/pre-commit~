#!/bin/sh

# Added for gofmt
STATUS=0
echo "--- START gofmt ---"
if !(gofmt -l -e .); then
    STATUS=1
fi

echo "--- END gofmt ---"

if [ $STATUS -eq 0 ]; then
    echo "OK!! ъ(ﾟДﾟ)ｸﾞｯｼﾞｮﾌﾞ!!"
else
    echo "NG!! チェックだ(σ・ω・)σYO！♪"
fi

exit $STATUS
